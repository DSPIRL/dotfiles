# Claude Code Adapter

This adapter is shaped for Claude Code projects.

## Contents

- `CLAUDE.md` provides root project instructions.
- `.claude/agents/` contains role prompts.
- `.claude/skills/` contains skill directories with `SKILL.md` files.

Use `templates/CONTEXT.md` as the project memory file when a project does not already have one.
